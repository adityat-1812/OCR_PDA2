import subprocess

# Run the first Python file
subprocess.run(["python", "crop.py"])

# Run the second Python file
subprocess.run(["python", "vision.py"])

# Run the third Python file
subprocess.run(["python", "remove.py"])